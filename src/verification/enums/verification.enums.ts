export enum VerificationType {
  IDENTITY = 'IDENTITY',
  DRIVING_LICENSE = 'DRIVING_LICENSE',
  VEHICLE = 'VEHICLE',
}

export enum VerificationStatus {
  PENDING = 'PENDING',
  IN_REVIEW = 'IN_REVIEW',
  VERIFIED = 'VERIFIED',
  REJECTED = 'REJECTED',
  EXPIRED = 'EXPIRED',
}
