export enum VehicleRcVerificationStatus {
  PENDING = 'PENDING',
  VALID = 'VALID',
  INVALID = 'INVALID',
  FAILED = 'FAILED',
}

export enum VehicleOwnerMatchStatus {
  MATCHED = 'MATCHED',
  NOT_MATCHED = 'NOT_MATCHED',
  NOT_CHECKED = 'NOT_CHECKED',
}
